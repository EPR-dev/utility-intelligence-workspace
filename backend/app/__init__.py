from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]
PROCESSED = ROOT / "data" / "processed"
CONFIG_DIR = ROOT / "config" / "networks"
SCHEMAS = ROOT / "schemas"
DEMO = ROOT / "data" / "demo"
